// _config.private.js — site-private overrides loaded after _config.js.
// This file IS committed (it contains no real data) so the browser never
// sees a 404. To set a real contact email, edit this file in your local
// clone and redeploy. Original model: this file was gitignored and the
// missing 404 was silently swallowed — that worked but produced a
// confusing "Failed to load resource" line in the browser console.
window.SITE_CONFIG = window.SITE_CONFIG || {};
// window.SITE_CONFIG.contactEmail = "your-real-address@example.com";
